# mega-cmd.fish

An unofficial set of fish completions for `mega-ls`, `mega-cp`, `mega-mv`, `mega-rm`,
`mega-put`, `mega-get`.

## Quirks

- `mega-put`: tab completions will display both local and remote paths if there
  is a forward slash at the start of the path
- `mega-cp`: you can't copy to email inbox
